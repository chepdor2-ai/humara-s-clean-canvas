{
  "name": "humara-workspace",
  "private": true,
  "scripts": {
    "dev": "sh -c 'cd humanizer-engine/frontend && if [ ! -d node_modules ]; then npm install; fi && npm run dev -- --hostname 0.0.0.0 --port ${PORT:-${npm_config_port:-3000}}'",
    "build:dev": "npm run build",
    "build": "cd humanizer-engine/frontend && npm install --include=dev && npm run build",
    "postinstall": "cd humanizer-engine/frontend && npm install --include=dev && npm run build",
    "start": "cd humanizer-engine/frontend && npx next start --hostname 0.0.0.0 --port ${PORT:-10000}"
  },
  "dependencies": {
    "next": "16.2.2",
    "compromise": "^14.15.0"
  }
}
