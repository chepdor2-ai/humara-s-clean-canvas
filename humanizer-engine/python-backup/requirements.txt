# Core (required)
fastapi>=0.104
uvicorn>=0.24
spacy>=3.7
textstat>=0.7
nltk>=3.8
python-dotenv>=1.0
pydantic>=2.4
setuptools

# Optional — needed for AI detector training & evaluation
# scikit-learn>=1.3
# numpy>=1.26
# pandas>=2.1
# datasets>=2.14

# Optional — needed for semantic guard (heavy, requires PyTorch)
# sentence-transformers>=2.2
# torch>=2.0
